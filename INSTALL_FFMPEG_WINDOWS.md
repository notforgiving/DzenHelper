# Подробная инструкция по установке ffmpeg на Windows

## Способ 1: Установка через готовый билд (рекомендуется)

### Шаг 1: Скачивание ffmpeg

1. Откройте браузер и перейдите на сайт: https://www.gyan.dev/ffmpeg/builds/
   - Это официальный сайт с готовыми билдами для Windows
   - Или альтернативно: https://ffmpeg.org/download.html → Windows → gyan.dev

2. Скачайте файл **ffmpeg-release-essentials.zip**
   - Выберите версию "release essentials" (самая популярная)
   - Файл будет называться примерно так: `ffmpeg-release-essentials.zip`

### Шаг 2: Распаковка архива

1. Найдите скачанный файл (обычно в папке "Загрузки")
2. Щелкните правой кнопкой мыши на файл → **Извлечь все...**
3. Выберите место для распаковки (например, `C:\ffmpeg`)
4. Нажмите **Извлечь**

### Шаг 3: Добавление в PATH

#### Вариант А: Через графический интерфейс (проще)

1. **Откройте "Переменные среды"**:
   - Нажмите `Win + R` (клавиша Windows + R)
   - Введите: `sysdm.cpl` и нажмите Enter
   - Или: Правой кнопкой на "Этот компьютер" → Свойства → Дополнительные параметры системы

2. **Откройте переменные среды**:
   - В открывшемся окне нажмите кнопку **"Переменные среды..."** (внизу)

3. **Найдите переменную Path**:
   - В разделе **"Системные переменные"** (нижняя часть окна)
   - Найдите переменную **Path**
   - Выделите её и нажмите **"Изменить..."**

4. **Добавьте путь к ffmpeg**:
   - Нажмите **"Создать"** (New)
   - Введите путь к папке `bin` внутри распакованной папки ffmpeg
   - Например: `C:\ffmpeg\ffmpeg-6.1.1-essentials_build\bin`
   - **Важно**: Путь должен вести к папке `bin`, где находится файл `ffmpeg.exe`
   - Нажмите **OK**

5. **Сохраните изменения**:
   - Нажмите **OK** во всех открытых окнах

#### Вариант Б: Через командную строку (для продвинутых)

1. Откройте **PowerShell** или **Командную строку** от имени администратора:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (администратор)" или "Терминал (администратор)"

2. Выполните команду (замените путь на ваш):
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\ffmpeg\ffmpeg-6.1.1-essentials_build\bin", [EnvironmentVariableTarget]::Machine)
   ```

### Шаг 4: Проверка установки

1. **Закройте все открытые окна терминала/командной строки**

2. **Откройте новое окно PowerShell или командной строки**:
   - Нажмите `Win + R`
   - Введите `cmd` или `powershell` и нажмите Enter

3. **Проверьте установку**:
   ```bash
   ffmpeg -version
   ```

4. **Если видите информацию о версии** - установка прошла успешно! ✅
   - Должна появиться информация о версии ffmpeg, например: `ffmpeg version 6.1.1`

5. **Если видите ошибку "не является внутренней или внешней командой"**:
   - Убедитесь, что вы добавили правильный путь к папке `bin`
   - Убедитесь, что перезапустили терминал после изменения PATH
   - Попробуйте перезагрузить компьютер

## Способ 2: Установка через Chocolatey (для продвинутых пользователей)

Если у вас установлен Chocolatey (менеджер пакетов для Windows):

1. Откройте PowerShell от имени администратора
2. Выполните команду:
   ```powershell
   choco install ffmpeg
   ```
3. Chocolatey автоматически установит ffmpeg и добавит его в PATH

## Способ 3: Установка через winget (Windows 10/11)

Если у вас Windows 10 версии 1809+ или Windows 11:

1. Откройте PowerShell или командную строку
2. Выполните команду:
   ```bash
   winget install ffmpeg
   ```
3. winget автоматически установит ffmpeg

## Проверка работы

После установки проверьте, что ffmpeg работает:

```bash
# Проверка версии
ffmpeg -version

# Проверка доступных кодеков
ffmpeg -codecs
```

## Устранение проблем

### Проблема: "ffmpeg не является внутренней или внешней командой"

**Решение:**
1. Проверьте, что путь к `bin` добавлен в PATH правильно
2. Убедитесь, что в папке `bin` есть файл `ffmpeg.exe`
3. Перезапустите терминал/командную строку
4. Перезагрузите компьютер (иногда требуется)

### Проблема: Не могу найти папку bin

**Решение:**
1. Откройте распакованную папку ffmpeg
2. Внутри должна быть папка с версией (например, `ffmpeg-6.1.1-essentials_build`)
3. Внутри этой папки должна быть папка `bin`
4. В папке `bin` должны быть файлы: `ffmpeg.exe`, `ffplay.exe`, `ffprobe.exe`

### Проблема: PATH не сохраняется

**Решение:**
1. Убедитесь, что вы редактируете системную переменную Path (не пользовательскую)
2. Убедитесь, что у вас есть права администратора
3. Попробуйте добавить путь через командную строку от имени администратора

## Дополнительная информация

- **Официальный сайт**: https://ffmpeg.org
- **Готовые билды для Windows**: https://www.gyan.dev/ffmpeg/builds/
- **Документация**: https://ffmpeg.org/documentation.html

## Быстрая проверка после установки

Откройте новый терминал и выполните:

```bash
ffmpeg -version
```

Если команда работает, значит ffmpeg успешно установлен и добавлен в PATH! 🎉






