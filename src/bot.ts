import 'dotenv/config';
import { Telegraf } from 'telegraf';
import { handleStart, handleHelp } from './handlers/commands';
import { SupabaseService } from './services/supabase';

const BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN;

if (!BOT_TOKEN) {
  console.error('❌ TELEGRAM_BOT_TOKEN не установлен в переменных окружения!');
  console.error('Создайте файл .env и добавьте TELEGRAM_BOT_TOKEN=your_token_here');
  process.exit(1);
}

const bot = new Telegraf(BOT_TOKEN, {
  // Устанавливаем таймаут обработчиков в 30 минут (запас, хотя операции простые)
  handlerTimeout: 1800000,
});

// Создаем один экземпляр сервиса Supabase для всего бота
const supabaseService = new SupabaseService();
// Создаем карту для хранения состояния пользователя по идентификатору чата
const userState = new Map<number, 'awaiting_schedule'>();

// Функция для вычисления ближайшего слота публикации на основе текущего времени и массива слотов
function calculateNextPublishAt(lastPublishAtIso: string | null, slots: number[]): Date {
  // Формируем итоговый массив слотов: если он пустой, используем значения по умолчанию
  const effectiveSlots = slots.length > 0 ? [...slots].sort((a, b) => a - b) : [10, 15, 18];
  // Создаем объект текущего времени, чтобы сравнивать с слотами
  const now = new Date();

  // Объявляем вспомогательную функцию для поиска ближайшего слота, начиная от текущего момента
  const findNextSlotFromNow = (): Date => {
    // Создаем дату для последующих вычислений на основе текущего времени
    const todayBase = new Date(now);
    // Проходим по всем слотам текущего дня
    for (const slotHour of effectiveSlots) {
      // Создаем дату для конкретного слота текущего дня
      const candidate = new Date(todayBase);
      // Устанавливаем час слота и обнуляем минуты, секунды и миллисекунды
      candidate.setHours(slotHour, 0, 0, 0);
      // Если слот еще впереди относительно текущего времени, сразу его возвращаем
      if (candidate > now) {
        return candidate;
      }
    }
    // Если все слоты сегодняшнего дня уже прошли, переключаемся на следующий день
    const nextDay = new Date(now);
    // Увеличиваем день на единицу, чтобы получить дату следующего дня
    nextDay.setDate(nextDay.getDate() + 1);
    // Устанавливаем первый слот следующего дня как первый элемент итогового массива слотов
    nextDay.setHours(effectiveSlots[0], 0, 0, 0);
    // Возвращаем вычисленную дату первого слота следующего дня
    return nextDay;
  };

  // Если предыдущей даты публикации нет вовсе, ищем ближайший слот от текущего момента
  if (!lastPublishAtIso) {
    // Вызываем вспомогательную функцию и возвращаем найденный ближайший слот
    return findNextSlotFromNow();
  }

  // Преобразуем ISO-строку последней публикации в объект Date
  const lastDate = new Date(lastPublishAtIso);
  // Если последняя дата публикации уже в прошлом или совпадает с текущим моментом
  if (lastDate <= now) {
    // В таком случае очередь считаем пустой и ищем ближайший свободный слот от текущего времени
    return findNextSlotFromNow();
  }

  // Получаем час последней публикации для определения индекса слота
  const lastHour = lastDate.getHours();
  // Ищем индекс слота, который соответствует часу последней публикации
  const currentSlotIndex = effectiveSlots.indexOf(lastHour);

  // Проверяем, что слот найден и он не является последним в массиве слотов
  if (currentSlotIndex !== -1 && currentSlotIndex < effectiveSlots.length - 1) {
    // Создаем копию даты последней публикации для вычисления следующего слота
    const sameDayNextSlot = new Date(lastDate);
    // Устанавливаем время на следующий слот того же дня
    sameDayNextSlot.setHours(effectiveSlots[currentSlotIndex + 1], 0, 0, 0);
    // Возвращаем вычисленную дату следующего слота текущего дня
    return sameDayNextSlot;
  }

  // Если час последней публикации не попадает в список слотов
  // или это был последний слот дня, планируем публикацию на следующий день
  const nextDayFirstSlot = new Date(lastDate);
  // Увеличиваем дату на один день, чтобы перейти к следующему дню
  nextDayFirstSlot.setDate(nextDayFirstSlot.getDate() + 1);
  // Устанавливаем время на первый слот следующего дня как первый элемент итогового массива слотов
  nextDayFirstSlot.setHours(effectiveSlots[0], 0, 0, 0);
  // Возвращаем дату первого слота следующего дня
  return nextDayFirstSlot;
}

// Функция инициализации бота и проверки подключения к Supabase
async function initialize() {
  try {
    // Проверяем подключение к Supabase через сервис
    const isConnected = await supabaseService.checkConnection();
    // Если подключение успешно, выводим соответствующее сообщение
    if (isConnected) {
      console.log('✅ Подключение к Supabase установлено');
    } else {
      // Если подключение не удалось, выводим предупреждение о возможной проблеме с переменными окружения
      console.warn('⚠️ Не удалось подключиться к Supabase. Проверьте переменные окружения SUPABASE_URL и SUPABASE_ANON_KEY');
    }
  } catch (error) {
    // Логируем ошибку проверки подключения к Supabase
    console.warn('⚠️ Ошибка при проверке подключения к Supabase:', error);
  }
}

// Регистрируем обработчик команды /start
bot.command('start', handleStart);
// Регистрируем обработчик команды /help
bot.command('help', handleHelp);

// Обработчик текстовых сообщений, который планирует посты
bot.on('text', async (ctx) => {
  // Если в контексте нет сообщения или оно не текстовое, выходим
  if (!ctx.message || !('text' in ctx.message)) {
    return;
  }

  // Получаем текст сообщения пользователя
  const text = ctx.message.text;
  // Получаем идентификатор чата, если он есть в контексте
  const chatId = ctx.chat?.id;

  // Если пользователь находится в режиме ввода нового расписания публикаций, обрабатываем ввод часов
  if (chatId !== undefined && userState.get(chatId) === 'awaiting_schedule') {
    try {
      // Разбиваем введенную строку на части по пробелам
      const parts = text.trim().split(/\s+/);
      // Создаем массив для хранения валидных чисел-часов
      const hours: number[] = [];

      // Проходим по всем частям, которые должны содержать часы
      for (const part of parts) {
        // Преобразуем текстовую часть в число
        const value = Number(part);
        // Проверяем, что значение является целым числом в диапазоне от 0 до 23
        if (!Number.isInteger(value) || value < 0 || value > 23) {
          // Если значение некорректно, отправляем пользователю сообщение с подсказкой формата
          await ctx.reply('❌ Часы должны быть целыми числами от 0 до 23. Пример: "9 13 20". Попробуйте еще раз.');
          // Прерываем дальнейшую обработку, не меняя расписание
          return;
        }
        // Добавляем корректное значение часа в массив
        hours.push(value);
      }

      // Удаляем дубликаты часов и сортируем их по возрастанию
      const uniqueSortedHours = Array.from(new Set(hours)).sort((a, b) => a - b);

      // Если после обработки массив оказался пустым, сообщаем об ошибке ввода
      if (uniqueSortedHours.length === 0) {
        // Отправляем пользователю подсказку о необходимости указать хотя бы один час
        await ctx.reply('❌ Нужно указать хотя бы один час. Пример: "9 13 20".');
        // Прерываем обработку без изменения расписания
        return;
      }

      // Сохраняем новые часы расписания в Supabase через сервис
      await supabaseService.replaceScheduleHours(uniqueSortedHours);

      // Формируем строку с перечислением новых часов расписания
      const hoursList = uniqueSortedHours.map((h) => `${h.toString().padStart(2, '0')}:00`).join(', ');
      // Отправляем пользователю подтверждение с новым расписанием
      await ctx.reply(`✅ Новое расписание публикаций сохранено: ${hoursList}`);

      // Удаляем состояние ожидания расписания для текущего пользователя
      userState.delete(chatId);
    } catch (error: any) {
      // Логируем ошибку обновления расписания публикаций
      console.error('Ошибка при обновлении расписания публикаций:', error);
      // Отправляем пользователю сообщение об ошибке без технических деталей
      await ctx.reply('❌ Не удалось сохранить новое расписание. Попробуйте позже.');
    }

    // После обработки ввода расписания выходим из обработчика
    return;
  }

  // Если пользователь нажал кнопку просмотра расписания, показываем список запланированных публикаций
  if (text === 'Посмотреть расписание') {
    try {
      // Получаем из базы данных все посты со статусом scheduled, запланированные на текущее и будущее время
      const scheduledPosts = await supabaseService.getScheduledPosts();

      // Если запланированных постов нет, информируем пользователя
      if (scheduledPosts.length === 0) {
        await ctx.reply('📭 Сейчас нет запланированных публикаций.');
      } else {
        // Формируем человекочитаемый список дат и времени публикаций
        const lines = scheduledPosts.map((post, index) => {
          // Преобразуем ISO-дату публикации в объект Date
          const publishDate = new Date(post.publish_at);
          // Форматируем дату и время публикации по локали ru-RU
          const formatted = publishDate.toLocaleString('ru-RU', {
            day: '2-digit',
            month: '2-digit',
            year: 'numeric',
            hour: '2-digit',
            minute: '2-digit',
          });
          // Возвращаем строку с порядковым номером и отформатированной датой публикации
          return `${index + 1}. ${formatted}`;
        });

        // Собираем итоговое сообщение со списком дат публикаций
        const message = '📅 Расписание запланированных публикаций:\n\n' + lines.join('\n');
        // Отправляем пользователю расписание
        await ctx.reply(message);
      }
    } catch (error: any) {
      // Логируем ошибку получения расписания
      console.error('Ошибка при получении расписания постов:', error);
      // Отправляем пользователю сообщение об ошибке без технических деталей
      await ctx.reply('❌ Не удалось получить расписание публикаций. Попробуйте позже.');
    }

    // После обработки кнопки выходим из обработчика
    return;
  }

  // Если пользователь нажал кнопку изменения расписания, запрашиваем новые часы публикаций
  if (text === 'Изменить расписание') {
    // Если идентификатор чата отсутствует, не можем продолжить диалог и просто выходим
    if (chatId === undefined) {
      return;
    }

    // Устанавливаем состояние пользователя в режим ожидания ввода часов расписания
    userState.set(chatId, 'awaiting_schedule');
    // Отправляем пользователю инструкцию по формату ввода часов
    await ctx.reply('🕒 Отправьте часы через пробел, в которые нужно планировать статьи. Пример: "9 13 20".');

    // После отправки инструкции выходим из обработчика
    return;
  }

  // Если сообщение начинается с символа /, считаем его командой и игнорируем здесь
  if (text.startsWith('/')) {
    return;
  }

  try {
    // Получаем последний пост из Supabase
    const lastPost = await supabaseService.getLastPost();
    // Получаем актуальное расписание часов публикаций из Supabase
    const scheduleHours = await supabaseService.getScheduleHours();
    // Вычисляем следующую дату публикации на основе последнего поста
    const nextPublishAt = calculateNextPublishAt(lastPost ? lastPost.publish_at : null, scheduleHours);

    // Вставляем новый пост в таблицу post с текстом и временем публикации
    const createdPost = await supabaseService.insertPost(text, nextPublishAt);

    // Преобразуем дату публикации в объект Date для форматирования
    const publishDate = new Date(createdPost.publish_at);

    // Форматируем дату публикации в строку вида DD.MM.YYYY HH:MM по локали ru-RU
    const formattedDate = publishDate.toLocaleString('ru-RU', {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
    });

    // Отправляем пользователю подтверждение с датой и временем публикации
    await ctx.reply(`✅ Пост запланирован на ${formattedDate} (статус: ${createdPost.status}).`);
  } catch (error: any) {
    // Логируем ошибку планирования поста
    console.error('Ошибка при планировании поста:', error);
    // Отправляем пользователю сообщение об ошибке без технических деталей
    await ctx.reply('❌ Не удалось запланировать пост. Попробуйте еще раз позже.');
  }
});

// Глобальный обработчик ошибок бота
bot.catch((err, ctx) => {
  // Логируем ошибку бота вместе с контекстом
  console.error('Ошибка в боте:', err);
  // Пытаемся отправить пользователю сообщение об ошибке
  ctx.reply('❌ Произошла ошибка. Попробуйте позже.');
});

// Функция запуска бота
async function start() {
  // Выполняем инициализацию (проверку Supabase)
  await initialize();

  // Логируем начало запуска бота
  console.log('🚀 Запуск Telegram бота...');
  // Логируем напоминание о необходимости корректной настройки Supabase
  console.log('📋 Убедитесь, что Supabase настроен (SUPABASE_URL и SUPABASE_ANON_KEY в .env)');

  // Запускаем бота и обрабатываем результат запуска
  bot.launch().then(() => {
    // Логируем успешный запуск бота
    console.log('✅ Бот успешно запущен!');
  }).catch((error) => {
    // Логируем ошибку запуска бота
    console.error('❌ Ошибка при запуске бота:', error);
    // Завершаем процесс с кодом ошибки
    process.exit(1);
  });
}

// Обрабатываем сигнал SIGINT для корректной остановки бота
process.once('SIGINT', () => bot.stop('SIGINT'));
// Обрабатываем сигнал SIGTERM для корректной остановки бота
process.once('SIGTERM', () => bot.stop('SIGTERM'));

// Запускаем основной процесс старта бота
start();