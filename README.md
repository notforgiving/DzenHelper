# DzenHelper - Telegram Bot для генерации статей из транскрипции

Telegram бот, который принимает текст транскрипции (может быть в нескольких сообщениях), генерирует статью и изображение, затем сохраняет результат в базу данных Supabase.

## Возможности

- 📝 Прием транскрипции несколькими сообщениями
- ✍️ Генерация структурированной статьи через Ollama
- 🎨 Генерация изображения через Hugging Face Inference API
- 💾 Сохранение статей в базу данных Supabase
- 📊 Управление статусами статей (опубликовано / готово к публикации)

## Требования

Перед запуском убедитесь, что установлены:

1. **Node.js** (версия 18 или выше)
2. **Ollama** - для генерации текста
3. **Supabase** - база данных (можно использовать бесплатный план)

## Установка зависимостей

### 1. Установка Ollama

1. Скачайте Ollama с https://ollama.ai
2. Установите Ollama (на Windows он запустится автоматически как служба)
3. **Важно на Windows**: Ollama запускается автоматически, не нужно запускать `ollama serve` вручную!
4. Загрузите модель:
   ```bash
   ollama pull llama3
   ```
5. Проверьте работу:
   ```bash
   ollama list
   ```

**Если видите ошибку "bind: Only one usage of each socket address"** - это нормально! Это означает, что Ollama уже запущен и работает.

### 2. Настройка Supabase

1. Создайте проект на https://supabase.com (можно использовать бесплатный план)
2. Перейдите в SQL Editor вашего проекта
3. Выполните SQL скрипт из файла `supabase_schema.sql` для создания таблицы `articles`
4. Получите URL и Anon Key из настроек проекта:
   - Settings → API
   - Скопируйте `Project URL` и `anon public` key

## Настройка проекта

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости Node.js:
   ```bash
   npm install
   ```

3. Создайте файл `.env`:
   ```bash
   cp .env.example .env
   ```
   
   Или создайте файл `.env` вручную со следующим содержимым:
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   SUPABASE_URL=your_supabase_project_url
   SUPABASE_ANON_KEY=your_supabase_anon_key
   OLLAMA_API_URL=http://localhost:11434
   OLLAMA_TEXT_MODEL=llama3
   HUGGINGFACE_API_TOKEN=your_token_here  # Опционально
   ```

4. Отредактируйте `.env` и добавьте необходимые токены:
   - **TELEGRAM_BOT_TOKEN**: Чтобы получить токен:
     - Откройте Telegram и найдите @BotFather
     - Отправьте команду `/newbot`
     - Следуйте инструкциям для создания бота
     - Скопируйте полученный токен в `.env`
   - **SUPABASE_URL**: URL вашего проекта Supabase
   - **SUPABASE_ANON_KEY**: Anon ключ из настроек Supabase
   - **OLLAMA_API_URL**: URL Ollama API (по умолчанию: http://localhost:11434)
   - **OLLAMA_TEXT_MODEL**: Модель для генерации текста (по умолчанию: llama3)
   - **HUGGINGFACE_API_TOKEN**: Опционально, для увеличения лимитов генерации изображений

## Запуск

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Продакшн:
```bash
npm run build
npm start
```

## Использование

1. Запустите бота
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start`
4. Отправьте текст транскрипции (можете отправлять несколькими сообщениями)
5. Когда закончите отправку транскрипции, отправьте команду `/finish`
6. Дождитесь обработки (может занять несколько минут)
7. Получите статью, изображение и подтверждение сохранения в базу данных

### Команды бота

- `/start` - начать работу с ботом
- `/help` - показать справку
- `/finish` - завершить отправку транскрипции и начать обработку
- `/cancel` - отменить текущую сессию транскрипции

## Структура проекта

```
DzenHelper/
├── src/
│   ├── bot.ts              # Основной файл бота
│   ├── prompts.ts           # Промпты для генерации статьи и изображения
│   ├── handlers/
│   │   ├── commands.ts      # Команды бота (/start, /help, /cancel)
│   │   └── transcription.ts # Обработка транскрипции
│   ├── services/
│   │   ├── article.ts      # Генерация статьи через Ollama
│   │   ├── image.ts        # Генерация изображения
│   │   └── supabase.ts     # Работа с базой данных Supabase
│   ├── utils/
│   │   ├── tempFiles.ts   # Управление временными файлами
│   │   └── telegram.ts    # Утилиты для Telegram
│   └── types/
│       └── index.ts       # TypeScript типы
├── temp/                  # Временные файлы (автоматически создается)
├── supabase_schema.sql    # SQL скрипт для создания таблицы
├── package.json
├── tsconfig.json
└── README.md
```

## Структура базы данных

Таблица `articles` содержит следующие поля:

- `id` (BIGSERIAL PRIMARY KEY) - уникальный идентификатор статьи
- `text` (TEXT NOT NULL) - текст статьи
- `status` (TEXT NOT NULL) - статус статьи: "опубликовано" или "готово к публикации"
- `created_at` (TIMESTAMP WITH TIME ZONE) - дата и время создания записи

## Переменные окружения

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (обязательно)
- `SUPABASE_URL` - URL проекта Supabase (обязательно)
- `SUPABASE_ANON_KEY` - Anon ключ Supabase (обязательно)
- `OLLAMA_API_URL` - URL Ollama API (по умолчанию: http://localhost:11434)
- `OLLAMA_TEXT_MODEL` - модель для генерации текста (по умолчанию: llama3)
- `IMAGE_MODEL` - модель для генерации изображений (по умолчанию: stabilityai/stable-diffusion-2-1)
- `HUGGINGFACE_API_TOKEN` - токен Hugging Face (опционально, для увеличения лимитов)

## Автопостинг через Supabase Edge Function `autopost`

### Как работает автопостинг

- Планирование постов:
  - Telegram-бот из `src/bot.ts` сохраняет посты в таблицу `post` с полями `text`, `publish_at` и статусом `scheduled`.
  - Время публикации рассчитывается на стороне бота.
- Публикация постов:
  - Supabase Edge Function `autopost` по расписанию выбирает из таблицы `post` все записи, у которых:
    - `status = 'scheduled'`
    - `publish_at <= now()`
  - Для каждой такой записи функция отправляет `text` в Telegram-канал и при успешной отправке обновляет статус на `published`.

### Где находится функция

- Код функции расположен в директории:
  - `supabase/functions/autopost/index.ts`

### Переменные окружения для Edge Function (Supabase Project Settings → Functions / Env vars)

- `SUPABASE_URL` — URL проекта Supabase.
- `SUPABASE_SERVICE_ROLE_KEY` — сервисный ключ с правами на чтение/запись в таблицу `post`.
- `TELEGRAM_BOT_TOKEN` — токен Telegram-бота (тот же, что используется локально).
- `TELEGRAM_CHAT_ID` — числовой ID канала, по умолчанию `-1003470782884`, можно переопределить в env.

### Настройка расписания (Scheduled Trigger)

- В Supabase создайте Scheduled Trigger:
  - Укажите функцию: `autopost`.
  - Задайте интервал, например: каждые 1–5 минут.
- Триггер вызывает функцию без тела запроса, функция сама определяет, какие посты нужно опубликовать.

### Формат ответа функции

- При успешной обработке функция возвращает JSON:
  - `publishedCount` — количество успешно опубликованных постов.
  - `failedCount` — количество постов, которые не удалось опубликовать или обновить.
- При ошибках конфигурации или внутренних сбоях возвращается JSON с полем `error`.

## Настройка промптов

Промпты для генерации статьи и изображения находятся в файле `src/prompts.ts`. Вы можете изменить их под свои нужды.

## Устранение проблем

### Бот не отвечает

- Проверьте, что токен бота правильный в `.env`
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### Ошибка генерации статьи

- Проверьте, что Ollama работает: `ollama list`
- Убедитесь, что модель загружена: `ollama pull llama3`
- На Windows: проверьте службу Ollama в "Службы" (services.msc)
- Проверьте логи Ollama на наличие ошибок

### Ошибка генерации изображения

- Проверьте интернет-соединение (Hugging Face API требует интернет)
- При необходимости создайте бесплатный токен на https://huggingface.co
- Добавьте токен в `.env`: `HUGGINGFACE_API_TOKEN=your_token`

### Ошибка сохранения в базу данных

- Проверьте, что переменные `SUPABASE_URL` и `SUPABASE_ANON_KEY` установлены в `.env`
- Убедитесь, что таблица `articles` создана в Supabase (выполните `supabase_schema.sql`)
- Проверьте права доступа в Supabase (таблица должна быть доступна для записи через anon key)

### Ollama не отвечает

- На Windows: Ollama должен запускаться автоматически как служба
- Проверьте службу Ollama в "Службы" (services.msc)
- Если служба не запущена, запустите приложение Ollama вручную
- Перезагрузите компьютер при необходимости

## Примечания

- Обработка транскрипции может занять значительное время в зависимости от длины текста
- Для генерации текста используется модель Ollama (по умолчанию llama3)
- Генерация изображений использует бесплатный Hugging Face Inference API
- Все временные файлы автоматически удаляются после обработки
- На Windows Ollama запускается автоматически как служба, не нужно запускать `ollama serve` вручную
- Сессия транскрипции автоматически завершается через 5 минут бездействия

## Лицензия

MIT
