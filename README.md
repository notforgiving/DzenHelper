# DzenHelper — Telegram‑бот‑планировщик постов для Дзена

Простой Telegram‑бот, который принимает текст поста, ставит его в общую очередь и сохраняет в базу данных Supabase.  
Публикация постов происходит по фиксированным временам в течение дня через Supabase Edge Function.

## Возможности

- 🧾 Приём текстовых постов одним сообщением.
- 🕒 Настраиваемые слоты публикации: по умолчанию **3 слота в день — 10:00, 15:00 и 18:00** (можно изменить через кнопку "Изменить расписание").
- 🕐 **Все время отображается в московском часовом поясе** — независимо от региона сервера.
- 📅 Умное расписание:
  - если в таблице `post` нет записей или последняя дата публикации уже в прошлом — бот находит **ближайший свободный слот** текущего дня;
  - если все слоты текущего дня уже прошли — пост попадает на **следующий день в первый слот**;
  - если есть будущие посты — новый ставится **в следующий слот после последнего запланированного**.
- 📚 Общая очередь для всех сообщений от всех пользователей.
- 📋 Просмотр расписания: кнопка "Посмотреть расписание" показывает все запланированные публикации с датой и временем (в московском времени).
- ⚙️ Настройка расписания: кнопка "Изменить расписание" позволяет задать свои часы публикации (например: "9 13 20").
- ❌ Отмена действий: можно отменить ввод нового расписания кнопкой "Отменить".
- 📸 Загрузка изображений в Google Drive: 
  - Отправьте фото с подписью (текстом статьи) — бот загрузит изображение в Google Drive, сохранит статью в базу данных вместе со ссылкой на изображение и запланирует публикацию.
  - Отправьте фото без подписи — бот загрузит изображение в Google Drive и вернет ссылку (пост в базу данных не сохраняется).
- 📝 Посты могут быть с изображением или без — текстовые посты сохраняются без изображения, посты с фото требуют подпись для сохранения в базу данных.
- 🤖 Автопостинг через Supabase Edge Function `autopost` в Telegram‑канал.

## Требования

Перед запуском убедитесь, что установлены:

1. **Node.js** (версия 18 или выше).
2. Аккаунт и проект в **Supabase** (можно бесплатный план).
3. Созданный **Telegram‑бот** через `@BotFather`.

## Настройка проекта

1. **Склонируйте репозиторий** или скачайте файлы.

2. **Установите зависимости** (на выбор `npm` или `yarn`):

   ```bash
   # через npm
   npm install

   # через yarn
   yarn
   ```

3. **Создайте файл `.env`** в корне проекта и заполните его:

   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   SUPABASE_URL=your_supabase_project_url
   SUPABASE_ANON_KEY=your_supabase_anon_key
   
   # Опционально: для работы с Google Drive
   GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
   GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
   ```

   - `TELEGRAM_BOT_TOKEN` — токен от `@BotFather`.
   - `SUPABASE_URL` — URL проекта Supabase.
   - `SUPABASE_ANON_KEY` — публичный anon‑ключ Supabase.
   - `GOOGLE_DRIVE_CREDENTIALS` — JSON с учетными данными сервисного аккаунта Google (опционально).
   - `GOOGLE_DRIVE_FOLDER_ID` — ID папки в Google Drive для загрузки файлов (опционально, если не указан — файлы загружаются в корень).

4. **Подготовьте таблицу `post` в Supabase**:

   Выполните SQL скрипт `supabase_post_schema.sql` в SQL Editor вашего Supabase проекта. Таблица будет создана автоматически со всеми необходимыми полями:

   - `id` — первичный ключ (`uuid`).
   - `text` — текст поста.
   - `publish_at` — дата и время запланированной публикации (`timestamp with time zone`).
   - `status` — статус поста (`scheduled` / `published`).
   - `created_at` — дата создания записи.
   - `image_url` — ссылка на изображение в Google Drive (опционально, для постов с изображениями).

## Запуск

### Режим разработки (с автоперезапуском через `ts-node`)

Из корня проекта:

```bash
# через npm
npm run dev

# через yarn
yarn dev
```

Для удобства на Windows добавлен файл `start-dev.bat`, который запускает `yarn dev` двойным кликом.

### Продакшн

Сборка и запуск собранной версии:

```bash
npm run build
npm start
```

Или эквивалентно через `yarn`:

```bash
yarn build
yarn start
```

## Использование

1. Запустите бота (`npm run dev` / `yarn dev` или `start-dev.bat` на Windows).
2. Откройте Telegram и найдите вашего бота.
3. Отправьте команду `/start`, чтобы получить краткую инструкцию.
4. **Отправьте текст сообщения** — бот сохранит его как пост БЕЗ изображения и рассчитает время публикации по слотовой логике.
5. В ответ бот напишет точную дату и время запланированной публикации (в московском времени).
6. **Отправьте фото с подписью (текстом статьи)** — бот автоматически:
   - Загрузит изображение в Google Drive (если настроен)
   - Сохранит статью (текст из подписи) в базу данных вместе со ссылкой на изображение
   - Запланирует публикацию поста по расписанию
   - Вернет подтверждение с датой публикации и ссылкой на изображение
7. **Отправьте фото БЕЗ подписи** — бот загрузит изображение в Google Drive и вернет ссылку, но НЕ сохранит пост в базу данных (только загрузка изображения).

### Кнопки бота

- **"Посмотреть расписание"** — показывает список всех запланированных публикаций с датой и временем (в московском времени). Работает всегда, даже если вы начали вводить новое расписание.
- **"Изменить расписание"** — позволяет задать свои часы публикации. После нажатия отправьте часы через пробел (например: "9 13 20"). Появится кнопка "Отменить" для выхода из режима ввода.
- **"Отменить"** — появляется при изменении расписания, позволяет отменить ввод и вернуться к обычному режиму.

### Команды бота

- `/start` — приветствие и краткая инструкция.
- `/help` — подробное описание логики планировщика и слотов.

## Структура проекта

```
DzenHelper/
├── src/
│   ├── bot.ts              # Основной файл бота‑планировщика
│   ├── handlers/
│   │   └── commands.ts     # Команды /start и /help
│   ├── services/
│   │   ├── supabase.ts     # Работа с таблицей post в Supabase
│   │   └── googledrive.ts  # Работа с Google Drive API
│   ├── types/
│   │   └── index.ts        # Общие типы
│   └── utils/
│       └── timezone.ts     # Утилиты для работы с московским временем
├── supabase/
│   └── functions/
│       └── autopost/
│           └── index.ts    # Edge Function для автопубликации постов
├── supabase_post_schema.sql  # SQL схема для таблицы post
├── supabase_schema.sql       # Пример схемы базы данных (legacy)
├── package.json
├── tsconfig.json
├── start-dev.bat           # Скрипт для быстрого запуска yarn dev на Windows
└── README.md
```

## Переменные окружения

- `TELEGRAM_BOT_TOKEN` — токен Telegram бота (обязательно).
- `SUPABASE_URL` — URL проекта Supabase (обязательно).
- `SUPABASE_ANON_KEY` — Anon ключ Supabase (обязательно).
- `GOOGLE_DRIVE_CREDENTIALS` — JSON с учетными данными сервисного аккаунта Google (опционально, для работы с Google Drive).
- `GOOGLE_DRIVE_FOLDER_ID` — ID папки в Google Drive для загрузки файлов (опционально, если не указан — файлы загружаются в корень).

## Настройка Google Drive (опционально)

Для работы с загрузкой изображений в Google Drive необходимо:

1. **Создать проект в Google Cloud Console**:
   - Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
   - Создайте новый проект или выберите существующий
   - Включите Google Drive API для проекта

2. **Создать сервисный аккаунт**:
   - Перейдите в "IAM & Admin" → "Service Accounts"
   - Создайте новый сервисный аккаунт
   - Скачайте JSON-ключ (credentials)

3. **Настроить папку для загрузки файлов** (обязательно):

   **Важно**: Сервисные аккаунты Google не имеют собственного хранилища. Используйте один из вариантов:

   **Вариант А: Обычная папка в вашем личном Google Drive** (рекомендуется, если нет доступа к Shared Drive):
   - Создайте папку в вашем личном Google Drive
   - Откройте папку → нажмите на иконку "Настройки доступа" (Share)
   - Нажмите "Добавить людей и группы"
   - Введите email сервисного аккаунта (находится в JSON-файле, поле `client_email`)
   - Выберите права доступа "Редактор" (Editor)
   - Нажмите "Отправить"
   - Скопируйте ID папки из URL (часть после `/folders/`)
   - Укажите этот ID в `GOOGLE_DRIVE_FOLDER_ID`

   **Вариант Б: Shared Drive (общий диск)** (если доступен):
   - Создайте Shared Drive в Google Drive:
     - Откройте Google Drive
     - Нажмите "Новый" → "Общий диск" (или "Shared Drive")
     - Дайте название диску (например, "Bot Files")
   - Добавьте сервисный аккаунт в Shared Drive:
     - Откройте настройки Shared Drive (правой кнопкой → "Управление участниками")
     - Нажмите "Добавить участников"
     - Введите email сервисного аккаунта (поле `client_email` из JSON)
     - Выберите роль "Content Manager" (Менеджер контента)
     - Нажмите "Отправить"
   - Создайте папку в Shared Drive (опционально):
     - Создайте папку внутри Shared Drive
     - Скопируйте ID папки из URL (часть после `/folders/`)
     - Укажите этот ID в `GOOGLE_DRIVE_FOLDER_ID`
   - **Примечание**: Если `GOOGLE_DRIVE_FOLDER_ID` не указан, файлы будут загружаться в корень Shared Drive

4. **Добавить переменные окружения**:
   - `GOOGLE_DRIVE_CREDENTIALS` — содержимое JSON-файла с учетными данными (в одну строку или как JSON-строка)
   - `GOOGLE_DRIVE_FOLDER_ID` — ID папки для загрузки (опционально)

**Пример настройки в `.env`**:
```env
GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"my-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"service-account@my-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}
GOOGLE_DRIVE_FOLDER_ID=1a2b3c4d5e6f7g8h9i0j
```

**Важно о сервисных аккаунтах**:
- Сервисные аккаунты Google **не имеют собственного хранилища**
- Необходимо использовать либо **обычную папку в вашем личном Google Drive** (с предоставленным доступом), либо **Shared Drive (общий диск)**
- Без настроенной папки с доступом загрузка файлов будет невозможна

**Примечание**: Если Google Drive не настроен, бот будет работать только с текстовыми постами. Функция загрузки изображений будет недоступна.

## Автопостинг через Supabase Edge Function `autopost`

### Как работает автопостинг

- Планирование постов:
  - Telegram-бот из `src/bot.ts` сохраняет посты в таблицу `post` с полями `text`, `publish_at` и статусом `scheduled`.
  - Время публикации рассчитывается на стороне бота по настраиваемым слотам (по умолчанию: 10:00, 15:00 и 18:00).
  - Все время отображается и вычисляется в московском часовом поясе (UTC+3).
- Публикация постов:
  - Supabase Edge Function `autopost` по расписанию выбирает из таблицы `post` все записи, у которых:
    - `status = 'scheduled'`
    - `publish_at <= now()`
  - Для каждой такой записи функция проверяет наличие поля `image_url`:
    - **Если `image_url` есть** — скачивает изображение из Google Drive и отправляет фото с текстом (подписью) в Telegram-канал
    - **Если `image_url` нет** — отправляет только текстовое сообщение в Telegram-канал
  - При успешной отправке функция обновляет статус поста на `published`.

### Где находится функция

- Код функции расположен в директории:
  - `supabase/functions/autopost/index.ts`

### Переменные окружения для Edge Function (Supabase Project Settings → Functions / Env vars)

- `SUPABASE_URL` — URL проекта Supabase.
- `SUPABASE_SERVICE_ROLE_KEY` — сервисный ключ с правами на чтение/запись в таблицу `post`.
- `TELEGRAM_BOT_TOKEN` — токен Telegram-бота (тот же, что используется локально).
- `TELEGRAM_CHAT_ID` — числовой ID канала, по умолчанию `-1003470782884`, можно переопределить в env.

### Настройка расписания (Scheduled Trigger)

- В Supabase создайте Scheduled Trigger:
  - Укажите функцию: `autopost`.
  - Задайте интервал, например: каждые 1–5 минут.
- Триггер вызывает функцию без тела запроса, функция сама определяет, какие посты нужно опубликовать.

### Формат ответа функции

- При успешной обработке функция возвращает JSON:
  - `publishedCount` — количество успешно опубликованных постов.
  - `failedCount` — количество постов, которые не удалось опубликовать или обновить.
- При ошибках конфигурации или внутренних сбоях возвращается JSON с полем `error`.

## Лицензия

MIT
