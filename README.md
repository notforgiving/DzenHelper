# DzenHelper - Telegram Bot для генерации статей из YouTube видео

Telegram бот, который принимает ссылку на YouTube видео, создает транскрипцию, генерирует статью и изображение.

## Возможности

- 📥 Скачивание видео с YouTube
- 🎵 Извлечение аудио из видео
- 📝 Транскрипция аудио в текст через Whisper
- ✍️ Генерация статьи через Ollama
- 🎨 Генерация изображения через Hugging Face Inference API

## Требования

Перед запуском убедитесь, что установлены:

1. **Node.js** (версия 18 или выше)
2. **yt-dlp** - для скачивания видео
3. **ffmpeg** - для обработки аудио/видео
4. **Ollama** - для генерации текста

## Установка зависимостей

### 1. Установка yt-dlp

```bash
# Windows (через pip)
pip install yt-dlp

# Linux/Mac
pip3 install yt-dlp
```

Проверка установки:
```bash
yt-dlp --version
```

### 2. Установка ffmpeg

#### Windows

**Способ 1: Готовый билд (рекомендуется)**

1. Скачайте `ffmpeg-release-essentials.zip` с https://www.gyan.dev/ffmpeg/builds/
2. Распакуйте архив (например, в `C:\ffmpeg`)
3. Добавьте папку `bin` в PATH:
   - Нажмите `Win + R` → введите `sysdm.cpl` → Enter
   - Переменные среды → Системные переменные → Path → Изменить
   - Создать → добавить путь к папке `bin` (например: `C:\ffmpeg\ffmpeg-6.1.1-essentials_build\bin`)
   - OK во всех окнах
4. Перезапустите терминал и проверьте: `ffmpeg -version`

**Способ 2: Через Chocolatey**
```powershell
choco install ffmpeg
```

**Способ 3: Через winget (Windows 10/11)**
```bash
winget install ffmpeg
```

#### Linux
```bash
sudo apt-get install ffmpeg
```

#### Mac
```bash
brew install ffmpeg
```

### 3. Установка Ollama

1. Скачайте Ollama с https://ollama.ai
2. Установите Ollama (на Windows он запустится автоматически как служба)
3. **Важно на Windows**: Ollama запускается автоматически, не нужно запускать `ollama serve` вручную!
4. Загрузите модель:
   ```bash
   ollama pull llama3
   ```
5. Проверьте работу:
   ```bash
   ollama list
   ```

**Если видите ошибку "bind: Only one usage of each socket address"** - это нормально! Это означает, что Ollama уже запущен и работает.

## Настройка проекта

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости Node.js:
   ```bash
   npm install
   ```

3. Создайте файл `.env`:
   ```bash
   cp .env.example .env
   ```

4. Отредактируйте `.env` и добавьте токен бота:
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   ```

   Чтобы получить токен:
   - Откройте Telegram и найдите @BotFather
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен в `.env`

5. Настройте опциональные переменные (если нужно):
   ```
   OLLAMA_API_URL=http://localhost:11434
   OLLAMA_TEXT_MODEL=llama3
   WHISPER_MODEL=Xenova/whisper-tiny
   HUGGINGFACE_API_TOKEN=your_token_here  # Опционально
   ```

## Настройка yt-dlp для работы с YouTube

Бот автоматически пробует несколько стратегий скачивания, но для лучшей работы рекомендуется настроить cookies.

### Автоматическое использование cookies из браузера

Бот автоматически пытается использовать cookies из браузера Chrome, Edge, Firefox или Brave. Если вы залогинены в YouTube в одном из этих браузеров, это должно работать автоматически.

### Ручная настройка cookies (если автоматическое не работает)

1. Установите расширение для экспорта cookies:
   - Chrome/Edge: [Get cookies.txt LOCALLY](https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpgggdhejndh)
   - Firefox: [cookies.txt](https://addons.mozilla.org/en-US/firefox/addon/cookies-txt/)

2. Откройте YouTube и войдите в свой аккаунт

3. Экспортируйте cookies в файл `cookies.txt`

4. Сохраните файл в корневую папку проекта

Бот автоматически обнаружит файл `cookies.txt` и будет использовать его.

### Убедитесь, что Node.js установлен

Бот использует Node.js как JavaScript runtime для yt-dlp. Проверьте установку:
```bash
node --version
```

Если Node.js не установлен, скачайте с https://nodejs.org

## Запуск

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Продакшн:
```bash
npm run build
npm start
```

## Использование

1. Запустите бота
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start`
4. Отправьте ссылку на YouTube видео
5. Дождитесь обработки (может занять несколько минут)
6. Получите статью и изображение

## Структура проекта

```
DzenHelper/
├── src/
│   ├── bot.ts              # Основной файл бота
│   ├── handlers/
│   │   ├── youtube.ts      # Обработка YouTube ссылок
│   │   └── commands.ts     # Команды бота
│   ├── services/
│   │   ├── downloader.ts   # Скачивание видео
│   │   ├── transcriber.ts # Транскрипция через Whisper
│   │   ├── article.ts     # Генерация статьи через Ollama
│   │   └── image.ts       # Генерация изображения
│   ├── utils/
│   │   ├── tempFiles.ts   # Управление временными файлами
│   │   └── telegram.ts    # Утилиты для Telegram
│   └── types/
│       └── index.ts       # TypeScript типы
├── temp/                  # Временные файлы (автоматически создается)
├── package.json
├── tsconfig.json
└── README.md
```

## Переменные окружения

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (обязательно)
- `OLLAMA_API_URL` - URL Ollama API (по умолчанию: http://localhost:11434)
- `OLLAMA_TEXT_MODEL` - модель для генерации текста (по умолчанию: llama3)
- `WHISPER_MODEL` - модель Whisper для транскрипции (по умолчанию: Xenova/whisper-tiny)
- `IMAGE_MODEL` - модель для генерации изображений (по умолчанию: stabilityai/stable-diffusion-2-1)
- `ARTICLE_PROMPT` - промпт для генерации статьи
- `IMAGE_PROMPT` - промпт для генерации изображения
- `TEMP_DIR` - директория для временных файлов (по умолчанию: ./temp)
- `HUGGINGFACE_API_TOKEN` - токен Hugging Face (опционально, для увеличения лимитов)

## Оптимизация производительности

### Реализованные оптимизации

1. **Скачивание только аудио** вместо видео (ускорение ~5-10x)
2. **Использование whisper-tiny** по умолчанию (ускорение ~2-3x)
3. **Оптимизация конвертации аудио** с оптимальным битрейтом

### Рекомендации для максимальной скорости

1. Используйте короткие видео (до 10 минут)
2. Используйте модель whisper-tiny (по умолчанию)
3. Закройте другие ресурсоемкие приложения
4. Используйте SSD вместо HDD

### Ожидаемое время обработки

С оптимизациями (whisper-tiny, только аудио):
- **5 минут видео:** ~2-3 минуты
- **10 минут видео:** ~4-6 минут
- **15 минут видео:** ~6-9 минут
- **30 минут видео:** ~12-18 минут

## Устранение проблем

### Бот не отвечает

- Проверьте, что токен бота правильный в `.env`
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### Ошибка скачивания YouTube видео

- Убедитесь, что yt-dlp установлен: `yt-dlp --version`
- Обновите yt-dlp: `pip install --upgrade yt-dlp`
- Убедитесь, что Node.js установлен (используется как JavaScript runtime)
- Используйте cookies из браузера (см. раздел "Настройка yt-dlp")
- Проверьте интернет-соединение

### Ошибка обработки аудио (ffmpeg)

- Проверьте, что ffmpeg установлен: `ffmpeg -version`
- Убедитесь, что папка `bin` из распакованного ffmpeg добавлена в PATH
- Перезапустите терминал после изменения PATH
- Перезагрузите компьютер (иногда требуется)

### Ошибка транскрипции

- Первый запуск загрузит модель Whisper (может занять время)
- Убедитесь, что есть интернет-соединение
- Проверьте, что есть достаточно места на диске для кэша моделей
- Для очень длинных видео (более 1 часа) транскрипция может занимать 10-30 минут

### Ошибка генерации статьи

- Проверьте, что Ollama работает: `ollama list`
- Убедитесь, что модель загружена: `ollama pull llama3`
- На Windows: проверьте службу Ollama в "Службы" (services.msc)
- Проверьте логи Ollama на наличие ошибок

### Ошибка генерации изображения

- Проверьте интернет-соединение (Hugging Face API требует интернет)
- При необходимости создайте бесплатный токен на https://huggingface.co
- Добавьте токен в `.env`: `HUGGINGFACE_API_TOKEN=your_token`

### Таймауты при обработке

- Для длинных видео транскрипция может занимать много времени
- Бот автоматически обновляет статус каждые 30 секунд
- Убедитесь, что достаточно оперативной памяти (рекомендуется минимум 4 ГБ)
- Используйте более легкую модель Whisper (whisper-tiny)

### Ollama не отвечает

- На Windows: Ollama должен запускаться автоматически как служба
- Проверьте службу Ollama в "Службы" (services.msc)
- Если служба не запущена, запустите приложение Ollama вручную
- Перезагрузите компьютер при необходимости

## Примечания

- Обработка видео может занять значительное время в зависимости от длины видео
- Для транскрипции используется модель Whisper-tiny по умолчанию (можно изменить через переменную окружения)
- Генерация изображений использует бесплатный Hugging Face Inference API
- Все временные файлы автоматически удаляются после обработки
- На Windows Ollama запускается автоматически как служба, не нужно запускать `ollama serve` вручную

## Лицензия

MIT
